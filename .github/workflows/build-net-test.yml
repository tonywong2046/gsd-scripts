# 已迁移到 GCP，此 OCI net-test workflow 已停用
name: '[DISABLED] Build net-test image'

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '此 workflow 已停用'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled.'
