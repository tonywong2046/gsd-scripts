# 已迁移到 GCP Cloud Run Functions，此 OCI Docker workflow 已停用
# 如需恢复请联系管理员
name: '[DISABLED] Build & Push Docker to OCIR'

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '此 workflow 已停用，请使用 deploy-gcp-functions.yml'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled. Use deploy-gcp-functions.yml instead.'
