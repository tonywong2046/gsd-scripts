# 已迁移到 GCP，此 OCI oci-ping workflow 已停用
name: '[DISABLED] Build oci-ping image'

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '此 workflow 已停用'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This workflow is disabled.'
